version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Running wp.py"
      - python --version
      - python wp.py

artifacts:
  files:
    - '**/*'
